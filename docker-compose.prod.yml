services:
  app:
    image: ${DROPMAIL_IMAGE:?Set DROPMAIL_IMAGE to the immutable GHCR tag}
    pull_policy: always

  nginx:
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
